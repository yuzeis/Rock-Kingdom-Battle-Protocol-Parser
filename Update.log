# RKPP Update

## Ver1.3 - Erneuerung

Base Version: Ver1.2 - Zusammenhalt  
Date: 2026-04-13

本文档记录 Ver1.2 到 Ver1.3 的实际变化。Ver1.2 的重点是 schema-driven 解码、语义 override 白名单、relay server 与 opencode 输出；Ver1.3 则在此基础上完成项目命名统一、本地离线数据包落地、数据敏感信息清理，以及目录下 live-decode 全流程修复。

---

## 1. 总体变化

Ver1.3 的核心目标是把 Ver1.2 已经搭好的 schema / relay / opencode 主路继续整理成更适合发布和维护的项目状态：

- 项目命名从 `RKBPP / rkbpp_*` 统一改为 `RKPP / rkpp_*`
- 对外项目名统一为 `Roco-Kingdom-Protocol-Parser`
- 运行时数据改为本项目 `Data/` 下的离线 JSON bundle
- 继续保留 Ver1.2 的 schema-driven 解码主路和语义 override 策略
- 强化 schema 解码后的宠物、技能、属性、Buff、技能池等名称补全
- 修复 live-decode 实跑中暴露的 outer record / TSF4G / c2s opcode / 心跳控制帧问题

---

## 2. 项目命名与文件名整理

Ver1.2 仍使用 `RKBPP` 和 `rkbpp_*` 命名。Ver1.3 已统一改为：

- 项目简称：`RKPP`
- Python 文件前缀：`rkpp_*`
- 入口脚本：`rkpp_live_tools.py`
- 核心模块：`rkpp_analyzer.py`、`rkpp_proto.py`、`rkpp_analysis.py`、`rkpp_reporter.py`、`rkpp_relay.py`
- 文档标题与说明：`Roco-Kingdom-Protocol-Parser`

这次整理的目的不是改变协议语义，而是为后续 Git 初始化、公开仓库命名和模块导入路径做统一。

---

## 3. 本地离线 Data bundle

Ver1.3 不再要求运行时直接从外部 `Roco-Kingdom-World-Data-main` 目录读取数据。当前策略是：

- 使用 `tools/build_data_bundle.py` 从外部资料转换出本项目最适合使用的 JSON 索引
- 生成后的索引只放在当前项目 `Data/` 下
- 运行时只读取本地 `Data/`
- `Data.py` 作为统一入口，优先加载 JSON bundle，并保留旧 CSV fallback 兼容

当前已接入的本地索引包括：

- `attr_map.json`
- `skill_map.json`
- `buff_map.json`
- `buffbase_map.json`
- `pet_map.json`
- `monster_map.json`
- `pet_skill_map.json`
- `monster_skillbank_map.json`
- `special_move_map.json`
- `opcode_pb_map.json`
- `pb_message_index.json`
- `data_manifest.json`

---

## 4. 数据敏感信息清理

本轮审查发现 `Data/data_manifest.json` 曾包含构建来源绝对路径。Ver1.3 已调整生成逻辑：

- 不再写入 `source_root`
- 不再写入本机用户目录
- 不再写入当前项目绝对路径
- manifest 仅保留 `generated_at`、`format`、`source_kind`、`files`

已重新扫描 `Data/`，确认未再命中以下敏感路径/标识：

- 本机用户目录
- 当前项目绝对路径
- 外部数据源目录名
- `source_root`

说明：`skill_map.json` 中仍可能保留 `/Game/ArtRes/...` 一类游戏资源路径；这不是本机路径，也不是当前项目路径，因此本轮没有按敏感信息删除。

---

## 5. 语义数据 enrich

Ver1.3 在 Ver1.2 schema 主路基础上继续补强了解码后的语义数据：

- `rkpp_analysis.py` 的 `_enrich_known_id_names()` 现在会补全 pet / skill / attr / buff / base skill pool / monster active skills 等信息
- `lookup_opcode()` 会合并本地 protobuf opcode 映射
- `decode_record()` 会输出 `pb_message_name`、`pb_full_name`、`pb_proto_file`
- `rkpp_proto.py` 的技能、Buff、BuffBase、宠物、动态技能与行动效果提取会附带更丰富的本地 meta
- `rkpp_reporter.py` 的战斗摘要能展示更多来自本地数据的名称与基础 ID

这部分延续 Ver1.2 的方向：简单字段翻译仍交给 schema，只有高价值语义提取保留在代码里。

---

## 6. live-decode 外层协议修复

使用目录下历史 pcap 进行 `live-decode --read-pcap` 后，Ver1.2 路径暴露出几个问题：

- c2s 中存在 `0x000101A9`、`0x00010133` 这类 32 位 opcode，低 16 位才对应现有语义表中的 `0x01A9`、`0x0133`
- s2c / c2s payload 末尾存在可变长度 `TSF4G` 尾巴，旧逻辑只兼容固定 `...tsf4g\x06`
- `0x013D / 0x013F` 心跳类包不是 protobuf 线格式，直接交给 `parse_proto_message()` 会产生伪 `root_clean=False`

Ver1.3 已修复：

- 新增 `normalize_c2s_opcode()`，将 `0x0001xxxx` 形式归一为低 16 位 opcode
- 保留 `raw_opcode`、`raw_opcode_hex`、`opcode_normalized`，避免丢失原始观察值
- `strip_tsf4g_padding()` 改为识别 `tsf4g<N>` 中最后 1 字节给出的总尾长
- 新增 `tsf4g_trailer_len()`，在 CSV 中暴露 `payload_trailer_len`
- 新增 `parse_special_payload()`，专门解析 `0x013D / 0x013F` 二进制定长心跳控制帧
- `opencode-server` 默认端口不可用时会自动回退到后续可用端口，避免交互式 `4 -> 2` 因 `8765` 被占用而直接退出

---

## 7. CSV 输出变化

`decoded_packets.csv` 在 Ver1.3 中新增以下外层审查字段：

- `raw_opcode`
- `raw_opcode_hex`
- `opcode_normalized`
- `payload_trailer_len`

这些字段主要用于后续逆向和抓包审查：

- `opcode_hex` 表示归一化后用于语义表和 schema 的 opcode
- `raw_opcode_hex` 表示线上解密体中的原始 32 位 opcode
- `opcode_normalized=True` 表示本行发生过 `0x0001xxxx -> 0xxxx` 归一化
- `payload_trailer_len` 表示本行被识别并剥离的 TSF4G 尾部长度

---

## 8. 测试与验证

当前单元测试已扩展到 24 项，覆盖：

- Data bundle 加载
- schema fallback
- 语义 override dispatch
- TSF4G 变长尾巴剥离
- c2s opcode 归一化
- `0x013D / 0x013F` 二进制心跳解析

运行结果：

- `python -m unittest discover -s tests`
- `Ran 24 tests`
- `OK`

目录下离线 live-decode 审查命令：

```powershell
python .\rkpp_live_tools.py live-decode --read-pcap .\rkpp_battle_analyze_20260411_154128\live_capture.pcap --key <REDACTED_KEY> --out-dir .\audit_live_decode_20260413_fix2
```

最终结果：

- `packets = 2048`
- `rows = 1209`
- `errors = 0`
- `listener_errors = 0`
- `unknown_opcode = 0`
- `root_clean=False = 0`

对比修复前同一份离线流：

- `unknown_opcode = 313 -> 0`
- `root_clean=False = 1156 -> 0`

---

## 9. 相比 Ver1.2 的核心结论

Ver1.2 已经完成了从硬编码主导到 schema 主路的切换；Ver1.3 的重点是把这条主路继续工程化：

- 命名更统一，便于 Git 仓库与模块维护
- 数据改为本地离线包，避免运行时依赖外部项目目录
- enrich 覆盖面更广，解码结果更可读
- live-decode 经过目录下真实 pcap 回放验证，外层解析和 protobuf 边界更稳
- Data 中的本机路径和外部项目路径残留已清理
